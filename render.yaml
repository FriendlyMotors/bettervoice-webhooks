services:
  - type: web
    name: bettervoice-webhook
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: LOG_LEVEL
        value: "INFO"
    branch: main
    plan: free